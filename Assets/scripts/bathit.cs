using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class bathit : MonoBehaviour
{
    [SerializeField] float distance;
    [SerializeField] GameObject ball;
    float movemagnitude=0;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
       
        //distance = Vector3.Distance(transform.position,ball.transform.position);
        //if (distance <= 100)
        //{

         

        //        transform.position = Vector3.MoveTowards(transform.position, ball.transform.position, 1);
               
        //}
    }
}
